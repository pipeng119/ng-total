import { BaCopyDirective } from './ba-copy.directive';

describe('BaCopyDirective', () => {
  it('should create an instance', () => {
    const directive = new BaCopyDirective();
    expect(directive).toBeTruthy();
  });
});
